export enum HtmlType {
  button = 'button',
  reset = 'reset',
  submit = 'submit',
}

export enum ButtonSize {
  xs = 'xs',
  sm = 'sm',
  base = 'base',
  lg = 'lg',
}

export enum ButtonVariant {
  primary = 'primary',
  secondary = 'secondary',
  ghost = 'ghost',
}
